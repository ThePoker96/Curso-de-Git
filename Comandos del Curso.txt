mkdir (NOmbre del directorio) : comando para crear una carpeta

touch (Nombre del archivo que no tiene nada por dentro <Ej: txt>) :

cat : comando para ver el contenido de un archivo

history : historia de los comandos

rm : comando para borrar los archivos

(comando --help) : ayuada de todas las opciones

git init : inicializa un repositorio

git status : muestra la informacion actual

git add (nombre de lo que se desea agregar)

git rm --cached (nombre del archivo a borrar cuando está añadido pero aun se encuentra en el intermedio)

git commit -m (envia los archivo añadido al repositorio)

git config

git config --list

git config --list --show-origin

git config --global user.name "Gabriel Pimienta"

git config --global user.email "gpimienta96@gmail.com"

git add . : añade todos los archivos modificados

git log <nombre del archivo> : (muestra el historia de cambios del archivo) 

git show <nombre del archivo> : (muestra el historial de cambios del archivo mas detallado)

git diff <indicador de una version <espacio> indicador de otra version> : (compara dos versiones)

git reset <indicador de una version> --hard or soft (regresar a una version anterior)

git log --stat

git checkout <indicador de version> <nombre del archivo> : (para ver un archivo especifico)

git checkout master <nombre del archivo> : (obtenemos el archivo mas actual)

git branch <nombre de la rama> : (crea una rama)

git checkout <nombre de la rama> : (cambias de rama)